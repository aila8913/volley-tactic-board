# 本機開發用的環境變數範本（issue #26）。
#
# 用法：複製成 .env（`cp .env.example .env`），再把 DATABASE_URL 換成你自己的連線字串。
# .env 本身在 .gitignore 裡，永遠不會進 git——因為連線字串含密碼，是憑證。
# 這個 .example 檔的存在意義就是：把「需要哪些變數」記錄下來，但不記錄「值是什麼」。
#
# 雲端部署（Render）不吃這個檔案，環境變數在 Render 儀表板上設定，見 docs/deploy.md。

# Postgres 連線字串。缺這個 lib/db/src/index.ts 會在啟動時直接丟錯（刻意的：
# 與其讓服務半殘地跑起來、等第一個查詢才炸，不如開機就失敗得明明白白）。
DATABASE_URL=postgres://<user>:<password>@localhost:5432/<db-name>

# Vite 前端 dev server 的埠號。
PORT=5173

# Vite 的 base path。本機是根目錄，部署到子路徑時才需要改。
BASE_PATH=/

# API server 的埠號。本機開發時前後端是兩個行程（Vite 5173、Express 3000），
# 所以要分開兩個變數；Vite 的 server.proxy 會把 /api/* 轉發到這個埠。
#
# 正式部署時**不設** API_PORT：那時只有一個行程，Express 同時吐 API 和前端靜態檔，
# 直接用平台注入的 PORT。見 artifacts/api-server/src/index.ts 的 `API_PORT ?? PORT`。
API_PORT=3000
